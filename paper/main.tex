taken from \citep{bereg08}:
Fr\'echet distance was first defined by Maurice Fr\'echet in 1906 \citep{frechet06}.
While known as a famous distance measure in the field of mathematics (more specifically, abstract spaces), it was Alt and Godau who first applied it in measuring the similarity of polygonal curves in early 1990s \citep{alt92a,alt92b}.
In 1994, Eiter and Mannila defined the discrete Fr\'echet distance between two polygonal chains A and B (in any fixed dimensions) \citep{eiter94}.
Recently, Jiang, Xu and Zhu applied the discrete Fr\'echet distance in aligning the backbones of proteins (which is called the protein structure-structure alignment problem) \citep{jiang08}.
In fact, in this application the discrete Fr\'echet distance makes more sense as the backbone of a protein is simply a polygonal chain in 3D, with each vertex being the alpha-carbon atom of a residue.
So if the (continuous) Fr\'echet distance is realized by an alpha-carbon atom and some other point which does not represent an atom, it is not meaningful biologically.
Jiang, et al.\ showed that given two planar polygonal chains the minimum discrete Fr\'echet distance between them, under both translation and rotation, can be computed in polynomial time.
They also applied some ideas therein to design an efficient heuristic for the original protein structure-structure alignment problem in 3D.

taken from \citep{buchin14}: It has been used for simplification \citep{agarwal05,bereg08}, clustering \citep{buchin11}, and map-matching \citep{alt03a,driemel12}.
The Fr\'echet distance also has applications in matching biological sequences \citep{wylie12}, analysing tracking data \citep{brakatsoulas05,buchin08}, and matching coastline data \citep{mascret06}.

taken from \citep{su20}: The time complexity of Fréchet distance is $\mathcal{O}(mn)$ \citep{eiter94}.

\citep{buchin14}: We show that, in the general case, the problem of computing the shortcut Fr\'echet distance is NP-hard.
This is the first hardness result for a variant of the Fr\'echet distance between two polygonal curves in the plane.

\citep{agarwal14}: Computing the discrete Fr\'echet distance in subquadratic time. ($\overset{?}{\sim}$ linear search faster than more educated approaches on modern hardware if input data is small)

\citep{aronov06}: Approximation of discrete Fr\'echet distance. Is \citep{avraham14} related?

\citep{bringmann14}: Fr\'echet distance has no strongly subquadratic algorithm unless SETH fails

\citep{gudmundsson18}: Approximation for long edges

Levenshtein distance: introduced by Vladimir Levenshtein \citep{levenshtein65}, matrix implementation \citep{wagner74}, fast: \citep{hirschberg75}

discrete Fr\'echet distance for handwriting recognition: \citep{sriraghavendra07}

\citep{zhu07}: Following the recent work of \citep{jiang08}, we investigate the protein local structural alignment problem using bounded discrete Fr\'echet distance

\citep{alt03b} showed that the Hausdorff distance and the Fr\'echet distance are the same for a pair of closed convex curves.

taken from \citep{ahn10}: In dimension $d$, given two polygonal curves with $n$ and $m$ vertices, respectively, there is a dynamic programming algorithm that computes the discrete Fr\'echet distance between them in $\mathcal{O}(dmn)$ time \citep{eiter94}.
Later, Aronov et al.\ presented efficient approximation algorithms for computing
the discrete Fr\'echet distance of two natural classes of curves: $\kappa$-bounded curves and backbone curves \citep{aronov06}.
They also proposed a pseudo-output-sensitive algorithm for computing the discrete Fr\'echet distance exactly.

\citep{ahn10}'s algorithm is based on a decision algorithm where the algorithm iterated in the inner-loop is a close adaptation of \citep{eiter94}.
They already use a matrix-formulation w/o recursion and they have eliminated most branches.

$d=2$ $\rightarrow$ \textit{planar case}

\begin{algorithm}
    \caption{Vanilla}
    \begin{algorithmic}[1]
        \Function{fr\'echet\_distance}{$p$: $\mathbb{R}^{P \times D}$, $q$: $\mathbb{R}^{Q \times D}$, $f$: $\mathbb{R}^D \times \mathbb{R}^D \mapsto \mathbb{R}$} $\to \mathbb{R}$
            \State $M$: $\mathbb{R}^{P \times Q}$ $\gets -1$
            \Statex
            \Function{eval}{i: $\mathbb{N}$, j: $\mathbb{N}$} $\to \mathbb{R}$ \Comment{Capture $p$, $q$, $f$, and $M$}
                \If{$M_{ij} > -1$}
                    \State \Return $M_{ij}$
                \EndIf
                \Statex
                \State $d$: $\mathbb{R}$ $\gets$ \Call{$f$}{$p_i, q_j$}
                \If{$i = 1 \;\land\; j = 1$}
                    \State $M_{ij} \gets d$
                \ElsIf{$i > 1 \;\land\; j = 1$}
                    \State $M_{ij} \gets \max\{\Call{eval}{i - 1, 1},\, d\}$
                \ElsIf{$i = 1 \;\land\; j > 1$}
                    \State $M_{ij} \gets \max\{\Call{eval}{1, j - 1},\, d\}$
                \Else
                    \State $M_{ij} \gets \max\{\min\{\Call{eval}{i - 1, j},\, \Call{eval}{i - 1, j - 1},\, \Call{eval}{i, j - 1}\},\, d\}$
                \EndIf
                \Statex
                \State \Return $M_{ij}$
            \EndFunction
            \Statex
            \State \Return \Call{eval}{$P - 1, Q - 1$}
        \EndFunction
    \end{algorithmic}
\end{algorithm}

\begin{algorithm}
    \caption{No recursion}
    \begin{algorithmic}[1]
        \Function{fr\'echet\_distance}{$p$: $\mathbb{R}^{P \times D}$, $q$: $\mathbb{R}^{Q \times D}$, $f$: $\mathbb{R}^D \times \mathbb{R}^D \mapsto \mathbb{R}$} $\to \mathbb{R}$
            \State $M$: $\mathbb{R}^{P \times Q}$ $\gets -1$
            \Statex
            \For{$i \gets 1,P$}
                \For{$j \gets 1,Q$}
                    \State $d$: $\mathbb{R}$ $\gets$ \Call{$f$}{$p_i, q_j$}
                    \If{$i = 1 \;\land\; j = 1$}
                        \State $M_{ij} \gets d$
                    \ElsIf{$i > 1 \;\land\; j = 1$}
                        \State $M_{ij} \gets \max\{M_{i-1,1},\, d\}$
                    \ElsIf{$i = 1 \;\land\; j > 1$}
                        \State $M_{ij} \gets \max\{M_{1,j-1},\, d\}$
                    \Else
                        \State $M_{ij} \gets \max\{\min\{M_{i-1,j},\, M_{i-1,j-1},\, M_{i,j-1}\},\, d\}$
                    \EndIf
                \EndFor
            \EndFor
            \Statex
            \State \Return $M_{P-1,Q-1}$   
        \EndFunction
    \end{algorithmic}
\end{algorithm}

\begin{algorithm}
    \caption{Linear memory}
    \begin{algorithmic}[1]
        \Function{fr\'echet\_distance}{$p$: $\mathbb{R}^{P \times D}$, $q$: $\mathbb{R}^{Q \times D}$, $f$: $\mathbb{R}^D \times \mathbb{R}^D \mapsto \mathbb{R}$} $\to \mathbb{R}$
            \State $d_1$: $\mathbb{R}^Q \gets$ \Call{$f$}{$p_1, q$}
            \State $v\phantom{{}_1}$: $\mathbb{R}^{Q+1} \gets \backslash\!\max(\infty, d_1)$ \Comment{scan using $\max\{\bullet, \bullet\}$}
            \Statex
            \For{$i \gets 2,P$}
                \State $d_i$: $\mathbb{R}^Q \gets$ \Call{$f$}{$p_i, q$} \Comment{apply $f$ poinwise}
                \State $u\phantom{{}_1}$: $\mathbb{R}^Q \gets \min\{ v_{1 \ldots Q},\, v_{2 \ldots Q+1} \}$ \Comment{pointwise $\min$}
                \Statex
                \For{$j \gets 1,Q$}
                    \State $v_{j+1} \gets \max\{ \min\{ v_j,\, u_j \},\, d_{ij} \}$
                \EndFor
            \EndFor
            \Statex
            \State \Return $v_{Q+1}$
        \EndFunction
    \end{algorithmic}
\end{algorithm}

\begin{algorithm}
    \caption{Scan}
    \begin{algorithmic}[1]
        \Function{fr\'echet\_maxmin}{$a$: $\mathbb{R}$, $x$: $\mathbb{R}^2$} $\to \mathbb{R}$
            \State \Return $\max\{ \min\{ a,\, x_1 \},\, x_2 \}$
        \EndFunction
        \Statex
        \Function{fr\'echet\_distance}{$p$: $\mathbb{R}^{P \times D}$, $q$: $\mathbb{R}^{Q \times D}$, $f$: $\mathbb{R}^D \times \mathbb{R}^D \mapsto \mathbb{R}$} $\to \mathbb{R}$
            \State $d_1$: $\mathbb{R}^Q \gets$ \Call{$f$}{$p_1, q$} \Comment{apply $f$ pointwise}
            \State $v\phantom{{}_1}$: $\mathbb{R}^{Q+1} \gets \backslash\!\max(\infty, d_1)$ \Comment{scan using $\max\{\bullet, \bullet\}$}
            \Statex
            \For{$i \gets 2,P$}
                \State $d_i$: $\mathbb{R}^Q \gets$ \Call{$f$}{$p_i, q$} \Comment{apply $f$ pointwise}
                \State $u\phantom{{}_1}$: $\mathbb{R}^Q \gets \min\{ v_{1 \ldots Q},\, v_{2 \ldots Q+1} \}$ \Comment{pointwise $\min$}
                \Statex
                \State $v \gets \backslash$\Call{fr\'echet\_maxmin}{$\infty,\, [u \;|\; d_i]$} \Comment{scan using \Call{fr\'echet\_maxmin}{}}
            \EndFor
            \Statex
            \State \Return $v_{Q+1}$
        \EndFunction
    \end{algorithmic}
\end{algorithm}

\begin{algorithm}
    \caption{Levenshtein}
    \begin{algorithmic}[1]
        \Function{levenshtein\_min}{$a$: $\mathbb{N}$, $x$: $\mathbb{N}^2$} $\to \mathbb{N}$
            \State \Return $\min\{ 1 + a,\, 1 + x_1,\, x_2 \}$
        \EndFunction
        \Statex
        \Function{levenshtein\_distance}{$p$: $\mathfrak{C}^{P \times D}$, $q$: $\mathfrak{C}^{Q \times D}$} $\to \mathbb{N}$
            \State $d_1$: $\mathbb{N}^Q \gets$ \Call{$\delta^{-1}$}{$p_1, q$} \Comment{apply Eq.~\eqref{eq:levenshtein_metric} pointwise}
            \State $v\phantom{{}_1}$: $\mathbb{N}^{Q+1} \gets \min(\mathbf{1}_Q, d_1) \,+\,[0, 1, \ldots, Q-1]$
            \Statex
            \For{$i \gets 2,P$}
                \State $d_i$: $\mathbb{N}^Q \gets$ \Call{$\delta^{-1}$}{$p_i, q$} \Comment{apply Eq.~\eqref{eq:levenshtein_metric} pointwise}
                \State $u\phantom{{}_1}$: $\mathbb{N}^Q \gets \min\{ v_{1 \ldots Q},\, v_{2 \ldots Q+1} \}$
                \Statex
                \State $v \gets \backslash$\Call{levenshtein\_min}{$i + 1,\, [u \;|\; d_i + v_{1 \ldots Q}]$} 
            \EndFor
            \Statex
            \State \Return $v_{Q+1}$
        \EndFunction
    \end{algorithmic}
\end{algorithm}

\begin{equation}
    \label{eq:levenshtein_metric}
    \delta^{-1}: \mathfrak{C} \times \mathfrak{C} \mapsto \mathbb{N}, \quad (a, b) \overset{\delta^{-1}}{\longmapsto} \begin{cases}
        0 & \text{if } a = b, \\
        \infty & \text{else.}
    \end{cases}
\end{equation}

\begin{algorithm}
    \caption{Fold/Scan}
    \begin{algorithmic}[1]
        \Function{fr\'echet\_maxmin}{$a$: $\mathbb{R}$, $x$: $\mathbb{R}^2$} $\to \mathbb{R}$
            \State \Return $\max\{ \min\{ a,\, x_1 \},\, x_2 \}$
        \EndFunction
        \Statex
        \Function{fr\'echet\_next}{$a$: $\mathbb{R}^{Q+1}$, $x$: $\mathbb{R}^{Q}$} $\to \mathbb{R}^{Q+1}$
            \State $u$: $\mathbb{R}^Q \gets \min\{ a_{1 \ldots Q},\, a_{2 \ldots Q+1} \}$ \Comment{pointwise $\min$}
            \State \Return $\backslash$\Call{fr\'echet\_maxmin}{$\infty,\, [u \;|\; x]$} \Comment{scan using \Call{fr\'echet\_maxmin}{}}
        \EndFunction
        \Statex
        \Function{fr\'echet\_distance}{$p$: $\mathbb{R}^{P \times D}$, $q$: $\mathbb{R}^{Q \times D}$, $f$: $\mathbb{R}^D \times \mathbb{R}^D \mapsto \mathbb{R}$} $\to \mathbb{R}$
            \State $d$: $\mathbb{R}^{P \times Q} \gets [\Call{\ensuremath{f}}{p_1, q} \;|\; \cdots \;|\; \Call{\ensuremath{f}}{p_P, q}]^\top$ \Comment{evaluate rows lazily}
            \State $v$: $\mathbb{R}^{Q+1} \gets \backslash\!\max(\infty, d_1)$ \Comment{scan using $\max\{\bullet, \bullet\}$}
            \State \Return $[$/\Call{fr\'echet\_next}{$v, d_{2 \ldots P}$}$]_{Q+1}$ \Comment{fold using \Call{fr\'echet\_next}{}}
        \EndFunction
    \end{algorithmic}
\end{algorithm}

\begin{algorithm}
    \caption{Scan}
    \label{alg:scan}
    \begin{algorithmic}[1]
        \Function{$\backslash[f: \mathcal{T}_2 \times \mathcal{T}_1 \to \mathcal{T}_2]$}{$init$: $\mathcal{T}_2$, $x$: $\mathcal{T}_1^N$} $\to \mathcal{T}_2^{N+1}$
            \State $y$: $\mathcal{T}_2^{N+1}$
            \State $y_1 \gets init$
            \Statex
            \State $t$: $\mathcal{T}_2$ $\gets init$
            \For{$i \gets 1,N$}
                \State $t \gets f(t, x_i)$
                \State $y_{i+1} \gets t$
            \EndFor
            \Statex
            \State \Return $y$
        \EndFunction
    \end{algorithmic}
\end{algorithm}

\begin{algorithm}
    \caption{Fold}
    \label{alg:fold}
    \begin{algorithmic}[1]
        \Function{$/[f: \mathcal{T}_2 \times \mathcal{T}_1 \to \mathcal{T}_2]$}{$init$: $\mathcal{T}_2$, $x$: $\mathcal{T}_1^N$} $\to \mathcal{T}_2$
            \State $t$: $\mathcal{T}_2$ $\gets init$
            \For{$i \gets 1,N$}
                \State $t \gets f(t, x_i)$
            \EndFor
            \Statex
            \State \Return $t$
        \EndFunction
    \end{algorithmic}
\end{algorithm}

\backmatter

\bmhead{Supplementary information}

If your article has accompanying supplementary file/s please state so here. 

Authors reporting data from electrophoretic gels and blots should supply the full unprocessed scans for key as part of their Supplementary information. This may be requested by the editorial team/s if it is missing.

Please refer to Journal-level guidance for any specific requirements.

\bmhead{Acknowledgments}

Acknowledgments are not compulsory. Where included they should be brief. Grant or contribution numbers may be acknowledged.

Please refer to Journal-level guidance for any specific requirements.

\section*{Declarations}

Some journals require declarations to be submitted in a standardised format. Please check the Instructions for Authors of the journal to which you are submitting to see if you need to complete this section. If yes, your manuscript must contain the following sections under the heading `Declarations':

\begin{itemize}
\item Funding
\item Conflict of interest/Competing interests (check journal-specific guidelines for which heading to use)
\item Ethics approval 
\item Consent to participate
\item Consent for publication
\item Availability of data and materials
\item Code availability 
\item Authors' contributions
\end{itemize}
